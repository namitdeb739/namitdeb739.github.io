name: Deploy to GitHub Pages

on:
    push:
        branches: [main]
    # Lets you re-deploy without pushing.
    workflow_dispatch:

# GITHUB_TOKEN needs these to publish a Pages artifact.
permissions:
    contents: read
    pages: write
    id-token: write

# One deploy at a time; don't cancel a run that is mid-publish.
concurrency:
    group: pages
    cancel-in-progress: false

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v6

            - uses: actions/setup-node@v6
              with:
                  node-version: 22
                  cache: npm

            # `npm ci` needs package-lock.json committed — it is.
            - run: npm ci

            - run: npm run build

            - uses: actions/configure-pages@v5

            - uses: actions/upload-pages-artifact@v4
              with:
                  path: ./dist

    deploy:
        needs: build
        runs-on: ubuntu-latest
        environment:
            name: github-pages
            # Not a shell context — no injection surface.
            url: ${{ steps.deployment.outputs.page_url }}
        steps:
            - id: deployment
              uses: actions/deploy-pages@v4
